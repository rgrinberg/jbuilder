(jbuild_version 1)

(library
 ((name varies)
  (wrapped false)
  (virtual_modules (varies))))

(alias
 ((name runtest)
  (deps (./fooexe/mainfoo.exe))
  (action (run ${<}))))

(alias
 ((name runtest)
  (deps (./bar/mainbar.exe))
  (action (run ${<}))))
