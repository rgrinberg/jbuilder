(ocamllex (extract_obj))

(library
 ((name configurator)
  (public_name dune.configurator)
  (libraries (stdune ocaml_config))
  (flags (:standard -safe-string %{read-lines:flags/flags.sexp}))
  (preprocess no_preprocessing)))
